[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.build]
cmds = ["cd frontend && npm ci && npm run build", "cd backend && npm ci --only=production"]

[start]
cmd = "node backend/src/server.js"
